interface TransactionInterface {
  id: ID
  amount: Int!
  method: String!
  member: Member! @relationship(type: "MADE", direction: IN)
  createdAt: DateTime!
  createdBy: String!
  transactionReference: String!
  transactionStatus: String!
  updatedAt: DateTime
}

type Transaction implements TransactionInterface {
  id: ID
  amount: Int!
  method: String!
  member: Member!
  createdAt: DateTime!
  createdBy: String!
  transactionReference: String!
  transactionStatus: String!
  updatedAt: DateTime
}

type Offering implements TransactionInterface {
  id: ID
  amount: Int!
  method: String!
  member: Member!
  createdAt: DateTime!
  createdBy: String!
  transactionReference: String!
  transactionStatus: String!
  updatedAt: DateTime
}

type Mutation {
  GiveBacentaOfferingMomo(
    memberEmail: String!
    amount: Float!
    bankingCode: Int!
    mobileNetwork: String!
    mobileNumber: String!
  ): Offering!
  GiveAnonBacentaOfferingMomo(
    amount: Float!
    bankingCode: Int!
    mobileNetwork: String!
    mobileNumber: String!
  ): Offering!
  SendTransactionOTP(otp: String!, reference: String!): Transaction!
  ConfirmTransaction(reference: String!): Offering!
}
